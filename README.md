# window-awesome-commands

### window + alt + r ==> for window screen recording
### window + . ==> for emoje
### window + shif + s ==> window screensort
### window + d  ==> for minimise application
### window + h ==> for voive typing.
